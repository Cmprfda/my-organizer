# User runtime data (CRITICAL: Do not commit)
status_overrides.json
notes.json
ccrs.json
todo.json
todo_done_archive.json
notepad.json
history.json
notepad_images/
bug_reports.json
tracker.log
projects_to_add
workbooks.json
repos.json
announcement.json
feedback_pending/
waiting.json
# registo das esperas resolvidas (ver store.WAITING_LOG_FILE): dados de quem usa
waiting_log.json
# retrato da ultima leitura boa da folha, para o arranque sem rede (tasks.py)
last_read.json
# a ancora do relatorio "antes da reuniao" (report.MEETING_FILE)
meeting_anchor.json
exports/
# copias do estado local (uma por hora nos dias recentes, ver cswaios/statefile.py)
backups/
# arquivo do historico, um ficheiro por mes (ver cswaios/history.py)
history/
# trincos entre processos (ficheiros vazios, ver statefile.state_lock)
*.lock
# temporarios da gravacao atomica (existem por milissegundos)
*.tmp

# Credentials / local Graph configuration (NEVER commit)
graph_config.json
graph_token.json
jira_config.json
chat_config.json
notify_config.json
team_config.json

# Workbooks (company data)
*.xlsx
*.xlsm

# Temporary test copies
*_TESTCOPY.xlsx

# Build output / release artefacts
.dist/
*.zip

# Local rollback snapshots
_rollback/

# Local tool settings
.claude/settings.local.json
.playwright-mcp/

# Python & Environment
__pycache__/
*.py[cod]
.venv/
venv/
.env

# OS Metadata
.DS_Store
Thumbs.db
