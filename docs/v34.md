## v34
- Nova fonte de dados: le o Excel diretamente do OneDrive/SharePoint pela API do Excel (Microsoft Graph), sem download e sem depender do ficheiro local.
- Definicoes (engrenagem) -> Dados: Automatico (OneDrive primeiro, ficheiro local em recurso), OneDrive (web) ou Ficheiro local.
- Ligacao a conta Microsoft feita nas Definicoes, com codigo no browser; as credenciais ficam so neste PC e a ligacao so pode ser iniciada a partir dele.
- Com a fonte OneDrive, as alteracoes de estado sao escritas pela API (deixa de ser preciso fechar o Excel).
- Sem configuracao (graph_config.json) nada muda: a app continua a funcionar exatamente como antes. Instrucoes no README.